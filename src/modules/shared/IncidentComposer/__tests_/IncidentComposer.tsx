/* import React from 'react';
import ReactDOM from 'react-dom';
import * as IncidentComposer from '../';

// tslint:disable jsx-no-lambda
it('renders without crashing', () => {
  const div = document.createElement('div');

  ReactDOM.render(
    <IncidentComposer
      position={{
        latitude: 1,
        longitude: 1,
      }}
      userId="1"
      onClose={_ => { return; }}
      onSave={_ => (__: any) => { return; } }
    />,
    div
  );
});
 */
